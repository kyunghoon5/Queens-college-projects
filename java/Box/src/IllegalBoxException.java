public class IllegalBoxException extends IllegalArgumentException{
	
	/*
	 * A constructor for an IllegalBoxException
	 * with one parameter
	 * @param message the message to display
	 * for the stack trace
	 */
	public IllegalBoxException(String message) {
		super(message);
	}
	
}